<?xml version="1.0" encoding="UTF-8"?>
<completions>
    <!-- Tags directly after "{% " -->
    <provider>
        <syntax>liquid-base</syntax>
        <selector>*:not(string,comment)</selector>
        <expression>(?&lt;=\{%\s)([a-zA-Z]*)</expression>
        <set>liquid.tags</set>
    </provider>

    <!-- Filters after "|" -->
    <provider>
        <syntax>liquid-base</syntax>
        <selector>*:not(string,comment)</selector>
        <expression>(?&lt;=\|)([a-zA-Z]*)</expression>
        <set>liquid.filters</set>
    </provider>
    
    <!-- Anything else after space, but not behind "is", "apply" or directly behind "{%" -->
    <provider>
        <syntax>liquid-base</syntax>
        <selector>*:not(string, comment)</selector>
        <expression>(?&lt;!is\s|apply\s|\{%\s|\S)([a-zA-Z]*)</expression>
        <set>liquid.functions</set>
        <set>liquid.keywords</set>
        <set>liquid.globals</set>
    </provider>

    <!-- Tags -->
    <set name="liquid.tags">
        <!-- Default: auto complete %} at the end -->
        <behavior suffix="\}">
            <append> %</append>
        </behavior>
        <behavior suffix="%\}">
            <append> </append>
        </behavior>
        <behavior suffix="(\s*%\})">
            <append></append>
        </behavior>
        <behavior>
            <append> %}</append>
        </behavior>
        <completion string="else" />
        <completion string="elseif">
            <behavior suffix="\}">
                <append> $[condition] %</append>
            </behavior>
            <behavior suffix="%\}">
                <append> $[condition] </append>
            </behavior>
            <behavior suffix="(\s*%\})">
                <append> $[condition]</append>
            </behavior>
            <behavior>
                <append> $[condition] %}</append>
            </behavior>
        </completion>
        <completion string="endfor" />
        <completion string="endif" />
        <completion string="if">
            <behavior suffix="\}">
                <append> $[condition] %}$[…]{% endif %</append>
            </behavior>
            <behavior suffix="%\}">
                <append> $[condition] %}$[…]{% endif </append>
            </behavior>
            <behavior suffix="(\s*%\})">
                <append> $[condition] %}$[…]{% endif</append>
            </behavior>
            <behavior>
                <append> $[condition] %}$[…]{% endif %}</append>
            </behavior>
        </completion>
        <completion string="render">
            <behavior suffix="\}">
                <append> '$[string file]' %</append>
            </behavior>
            <behavior suffix="%\}">
                <append> '$[string file]' </append>
            </behavior>
            <behavior suffix="(\s*%\})">
                <append> '$[string file]'</append>
            </behavior>
            <behavior>
                <append> '$[string file]' %}</append>
            </behavior>
        </completion>
        <completion string="render">
            <behavior suffix="\}">
                <append> '$[string file]' with $[data] %</append>
            </behavior>
            <behavior suffix="%\}">
                <append> '$[string file]' with $[data] </append>
            </behavior>
            <behavior suffix="(\s*%\})">
                <append> '$[string file]' with $[data]</append>
            </behavior>
            <behavior>
                <append> '$[string file]' with $[data] %}</append>
            </behavior>
        </completion>
    </set>

    <!-- Keywords -->
    <set name="liquid.keywords">
        <!-- Regular liquid -->
        <completion string="not" />
        <completion string="as" />
        <completion string="in" />
        <completion string="is" />
        <completion string="with">
            <behavior>
                <append> $[name] as $[variable]</append>
            </behavior>
        </completion>
        <completion string="with">
            <behavior>
                <append> $[data]</append>
            </behavior>
        </completion>
    </set>
    
    <!-- Globals -->
    <set name="liquid.globals">
        <completion string="section" />
        <completion string="settings" />
    </set>

    <!-- Filters -->
    <set name="liquid.filters">
        <completion string="abs" />
        <completion string="batch" symbol="function">
            <behavior>
                <append>($[int size], $[string fill], $[bool preserve_keys])</append>
            </behavior>
        </completion>
        <completion string="capitalize" />
        <completion string="column" symbol="function">
            <behavior>
                <append>($[string name])</append>
            </behavior>
        </completion>
        <completion string="convert_encoding" symbol="function">
            <behavior>
                <append>($[string to], $[string from])</append>
            </behavior>
        </completion>
        <completion string="country_name" symbol="function">
            <behavior>
                <append>($[string locale])</append>
            </behavior>
        </completion>
        <completion string="currency_name" symbol="function">
            <behavior>
                <append>($[string locale])</append>
            </behavior>
        </completion>
        <completion string="currency_symbol" symbol="function">
            <behavior>
                <append>($[string locale])</append>
            </behavior>
        </completion>
        <completion string="data_uri" symbol="function">
            <behavior>
                <append>($[string mime], $[array parameters])</append>
            </behavior>
        </completion>
        <completion string="date" symbol="function">
            <behavior>
                <append>($[string format], $[string timezone])</append>
            </behavior>
        </completion>
        <completion string="date_modify" symbol="function">
            <behavior>
                <append>($[string modifier])</append>
            </behavior>
        </completion>
        <completion string="default" symbol="function">
            <behavior>
                <append>($[mixed default])</append>
            </behavior>
        </completion>
        <completion string="escape" />
        <completion string="escape" symbol="function">
            <behavior>
                <append>($[string strategy], $[string charset])</append>
            </behavior>
        </completion>
        <completion string="e" />
        <completion string="e" symbol="function">
            <behavior>
                <append>($[string strategy], $[string charset])</append>
            </behavior>
        </completion>
        <completion string="filter" symbol="function">
            <behavior>
                <append>($[arguments] => $[function])</append>
            </behavior>
        </completion>
        <completion string="first" />
        <completion string="format" symbol="function">
            <behavior>
                <append>($[])</append>
            </behavior>
        </completion>
        <completion string="format_currency" symbol="function">
            <behavior>
                <append>($[string currency], $[object attributes], $[string locale])</append>
            </behavior>
        </completion>
        <completion string="format_date" symbol="function">
            <behavior>
                <append>($[string dateFormat], locale=$[string locale])</append>
            </behavior>
        </completion>
        <completion string="format_date" symbol="function">
            <behavior>
                <append>(pattern=$[string pattern])</append>
            </behavior>
        </completion>
        <completion string="format_datetime" symbol="function">
            <behavior>
                <append>($[string dateFormat], $[string timeFormat], locale=$[string locale])</append>
            </behavior>
        </completion>
        <completion string="format_datetime" symbol="function">
            <behavior>
                <append>(pattern=$[string pattern])</append>
            </behavior>
        </completion>
        <completion string="format_number" symbol="function">
            <behavior>
                <append>($[object attrs])</append>
            </behavior>
        </completion>
        <completion string="format_number" symbol="function">
            <behavior>
                <append>(style=$[string style])</append>
            </behavior>
        </completion>
        <completion string="format_number" symbol="function">
            <behavior>
                <append>(locale=$[string locale])</append>
            </behavior>
        </completion>
        <completion string="format_time" symbol="function">
            <behavior>
                <append>($[string timeFormat], locale=$[string locale])</append>
            </behavior>
        </completion>
        <completion string="format_time" symbol="function">
            <behavior>
                <append>(pattern=$[string pattern])</append>
            </behavior>
        </completion>
        <completion string="html_to_markdown" />
        <completion string="inky_to_html" />
        <completion string="inline_css" />
        <completion string="inline_css" symbol="function">
            <behavior>
                <append>(source($[string source]))</append>
            </behavior>
        </completion>
        <completion string="join" symbol="function">
            <behavior>
                <append>($[string glue], $[string end])</append>
            </behavior>
        </completion>
        <completion string="json_encode" />
        <completion string="json_encode" symbol="function">
            <behavior>
                <append>(constant($[string options]))</append>
            </behavior>
        </completion>
        <completion string="keys" />
        <completion string="language_name" symbol="function">
            <behavior>
                <append>($[string locale])</append>
            </behavior>
        </completion>
        <completion string="last" />
        <completion string="length" />
        <completion string="locale_name" symbol="function">
            <behavior>
                <append>($[string locale])</append>
            </behavior>
        </completion>
        <completion string="lower" />
        <completion string="map" symbol="function">
            <behavior>
                <append>($[arguments] => $[function])</append>
            </behavior>
        </completion>
        <completion string="markdown_to_html" />
        <completion string="merge" symbol="function">
            <behavior>
                <append>($[array data])</append>
            </behavior>
        </completion>
        <completion string="nl2br" />
        <completion string="number_format" symbol="function">
            <behavior>
                <append>($[int decimals], $[string decimal_point], $[string thousand_sep])</append>
            </behavior>
        </completion>
        <completion string="raw" />
        <completion string="reduce" symbol="function">
            <behavior>
                <append>($[arguments] => $[function])</append>
            </behavior>
        </completion>
        <completion string="replace" symbol="function">
            <behavior>
                <append>($[object from])</append>
            </behavior>
        </completion>
        <completion string="reverse" />
        <completion string="reverse" symbol="function">
            <behavior>
                <append>($[bool preserve_keys])</append>
            </behavior>
        </completion>
        <completion string="round" />
        <completion string="round" symbol="function">
            <behavior>
                <append>($[int precision], $[string method])</append>
            </behavior>
        </completion>
        <completion string="slice" symbol="function">
            <behavior>
                <append>($[int start], $[int length], $[bool preserve_keys])</append>
            </behavior>
        </completion>
        <completion string="sort" symbol="function">
            <behavior>
                <append>($[arguments] => $[function])</append>
            </behavior>
        </completion>
        <completion string="spaceless" />
        <completion string="split" symbol="function">
            <behavior>
                <append>($[string delimiter], $[int limit])</append>
            </behavior>
        </completion>
        <completion string="striptags" />
        <completion string="striptags" symbol="function">
            <behavior>
                <append>($[string allowable_tags])</append>
            </behavior>
        </completion>
        <completion string="timezone_name" symbol="function">
            <behavior>
                <append>($[string locale])</append>
            </behavior>
        </completion>
        <completion string="title" />
        <completion string="trim" />
        <completion string="trim" symbol="function">
            <behavior>
                <append>($[string character_mask], $[string side)</append>
            </behavior>
        </completion>
        <completion string="trim" symbol="function">
            <behavior>
                <append>(side=$[string side)</append>
            </behavior>
        </completion>
        <completion string="u" />
        <completion string="upper" />
        <completion string="url_encode" />
        <!-- Craft CMS -->
        <completion string="append" symbol="function">
            <behavior>
                <append>($[string html])</append>
            </behavior>
        </completion>
        <completion string="append" symbol="function">
            <behavior>
                <append>($[string html], 'keep')</append>
            </behavior>
        </completion>
        <completion string="append" symbol="function">
            <behavior>
                <append>($[string html], 'replace')</append>
            </behavior>
        </completion>
        <completion string="ascii" />
        <completion string="ascii" symbol="function">
            <behavior>
                <append>($[string locale])</append>
            </behavior>
        </completion>
        <completion string="atom" />
        <completion string="attr" symbol="function">
            <behavior>
                <append>($[object attributes])</append>
            </behavior>
        </completion>
        <completion string="camel" />
        <completion string="contains" symbol="function">
            <behavior>
                <append>($[string key], $[mixed value])</append>
            </behavior>
        </completion>
        <completion string="currency" symbol="function">
            <behavior>
                <append>($[string currency])</append>
            </behavior>
        </completion>
        <completion string="currency" symbol="function">
            <behavior>
                <append>($[string currency], stripZeros=true)</append>
            </behavior>
        </completion>
        <completion string="diff" symbol="function">
            <behavior>
                <append>($[array array])</append>
            </behavior>
        </completion>
        <completion string="duration" symbol="function">
            <behavior>
                <append>($[boolean showSeconds])</append>
            </behavior>
        </completion>
        <completion string="encenc" />
        <completion string="explodeClass" />
        <completion string="explodeStyle" />
        <completion string="filesize" />
        <completion string="group" symbol="function">
            <behavior>
                <append>($[arguments] => $[function])</append>
            </behavior>
        </completion>
        <completion string="hash" />
        <completion string="id" />
        <completion string="index" symbol="function">
            <behavior>
                <append>($[string key])</append>
            </behavior>
        </completion>
        <completion string="indexOf" symbol="function">
            <behavior>
                <append>($[string value])</append>
            </behavior>
        </completion>
        <completion string="intersect" symbol="function">
            <behavior>
                <append>($[array array])</append>
            </behavior>
        </completion>
        <completion string="json_decode" />
        <completion string="kebab" />
        <completion string="lcfirst" />
        <completion string="literal" />
        <completion string="markdown" />
        <completion string="markdown" symbol="function">
            <behavior>
                <append>($[string flavor], $[bool inlineOnly])</append>
            </behavior>
        </completion>
        <completion string="md" />
        <completion string="md" symbol="function">
            <behavior>
                <append>($[string flavor], $[bool inlineOnly])</append>
            </behavior>
        </completion>
        <completion string="multisort" symbol="function">
            <behavior>
                <append>($[mixed properties], sortFlag=$[const sortFlag])</append>
            </behavior>
        </completion>
        <completion string="multisort" symbol="function">
            <behavior>
                <append>($[arguments] => $[function])</append>
            </behavior>
        </completion>
        <completion string="namespace" symbol="function">
            <behavior>
                <append>($[string namespace])</append>
            </behavior>
        </completion>
        <completion string="namespaceInputId" symbol="function">
            <behavior>
                <append>($[string namespace])</append>
            </behavior>
        </completion>
        <completion string="namespaceInputName" symbol="function">
            <behavior>
                <append>($[string namespace])</append>
            </behavior>
        </completion>
        <completion string="number" />
        <completion string="number" symbol="function">
            <behavior>
                <append>(false)</append>
            </behavior>
        </completion>
        <completion string="parseRefs" />
        <completion string="pascal" />
        <completion string="percentage" />
        <completion string="prepend" symbol="function">
            <behavior>
                <append>($[string html])</append>
            </behavior>
        </completion>
        <completion string="prepend" symbol="function">
            <behavior>
                <append>($[string html], 'keep')</append>
            </behavior>
        </completion>
        <completion string="prepend" symbol="function">
            <behavior>
                <append>($[string html], 'replace')</append>
            </behavior>
        </completion>
        <completion string="purify" />
        <completion string="purify" symbol="function">
            <behavior>
                <append>($[string config])</append>
            </behavior>
        </completion>
        <completion string="push" symbol="function">
            <behavior>
                <append>($[items])</append>
            </behavior>
        </completion>
        <completion string="rss" />
        <completion string="time" symbol="function">
            <behavior>
                <append>($[string format], timezone=$[string timezone])</append>
            </behavior>
        </completion>
        <completion string="time" symbol="function">
            <behavior>
                <append>($[string format], locale=$[string locale])</append>
            </behavior>
        </completion>
        <completion string="timestamp" />
        <completion string="truncate" symbol="function">
            <behavior>
                <append>($[int chars], $[string append])</append>
            </behavior>
        </completion>
        <completion string="ucfirst" />
        <completion string="unique" />
        <completion string="unshift" symbol="function">
            <behavior>
                <append>($[items])</append>
            </behavior>
        </completion>
        <completion string="values" />
        <completion string="without" symbol="function">
            <behavior>
                <append>($[elements])</append>
            </behavior>
        </completion>
        <completion string="withoutKey" symbol="function">
            <behavior>
                <append>($[string key])</append>
            </behavior>
        </completion>
    </set>
</completions>